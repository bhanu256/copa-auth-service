server:
  port: 8100
  
spring:
  application:
    name: AUTH-SERVICE
  datasource:
    url: "jdbc:mysql://localhost:3306/copa"
    username: root
    password: chowd@ry1
  jpa:
    hibernate:
      ddl-auto: update
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url: 
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost
  
 